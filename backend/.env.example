PORT=5000
MONGO_URI=mongodb+srv://<user>:<pass>@cluster.mongodb.net/mytrade
CACHE_TTL_MINUTES=60
CRON_SCHEDULE=0 */2 * * *
# NEWS_API_KEY=        (optional — leave blank until ready to wire news)
# NEWS_PROVIDER=newsapi
